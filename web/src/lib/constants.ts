/** App-wide knobs (pinned cap). Free-generation defaults live in `./entitlements` for billing alignment. */

export const MAX_PINNED_FACTS = 15;

export { FREE_GENERATIONS_PER_MONTH } from "./entitlements";
